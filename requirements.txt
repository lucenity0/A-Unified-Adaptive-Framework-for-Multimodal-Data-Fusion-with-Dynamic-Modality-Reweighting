# Install torch FIRST, matched to your hardware (see WINDOWS.md / pytorch.org):
#   NVIDIA GPU : pip install torch --index-url https://download.pytorch.org/whl/cu126
#   CPU only   : pip install torch
# Any torch >= 2.1 works; the pipeline was developed against torch 2.10.
transformers==5.3.0
scikit-learn>=1.4
scipy>=1.11
pandas>=2.0
pyarrow>=14.0
pillow>=10.0
tqdm
optuna>=4.0        # only needed for ensemble.py
